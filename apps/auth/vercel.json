{
  "buildCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && pnpm build --filter=auth",
  "installCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
